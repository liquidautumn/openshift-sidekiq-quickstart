#!/bin/bash

set -e

pushd ${OPENSHIFT_REPO_DIR} > /dev/null

echo "Starting sidekiq..."
bundle exec "sidekiq -d -r ./workers.rb -P '${OPENSHIFT_DATA_DIR}sidekiq.pid' -L '${OPENSHIFT_DATA_DIR}sidekiq.log'"

popd
